node_modules/
dist/
release/
resources/
verify-shots/

# local machine's MCP server config (references a local AWS profile name,
# not committed since other clones/machines will have their own)
/.mcp.json
